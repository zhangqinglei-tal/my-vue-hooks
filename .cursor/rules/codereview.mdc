---
description: 代码 review 时检查
globs: 
alwaysApply: false
---
# 代码 Review 检查清单

## 使用说明
请按照以下分类逐项检查，确保代码质量、安全性和可维护性。根据项目实际情况，某些检查项可选择性执行。

---

## 基础规范检查
- 是否遵循 [vue.mdc](mdc:.cursor/rules/vue.mdc)
- 是否遵循 [typescript.mdc](mdc:.cursor/rules/typescript.mdc)
- 是否遵循 [env.d.ts](mdc:env.d.ts)
- 是否遵循 [router.mdc](mdc:.cursor/rules/router.mdc)
- 是否遵循 [structure.mdc](mdc:.cursor/rules/structure.mdc)
- 是否遵循 [comment.mdc](mdc:.cursor/rules/comment.mdc)
- 不要包含任何非utf8字符(如emoji表情)，否则 gitlab merge 会报错

---

## TypeScript 类型安全检查

### 类型使用规范
- 是否滥用 `any` 类型（应使用具体类型或 `unknown`）
- 是否过度使用类型断言 `as`（应优先使用类型守卫）
- 是否有 `@ts-ignore` 或 `@ts-nocheck` 注释（需要合理解释）
- 是否使用了 `object` 类型（应使用具体接口）

### 类型定义完整性
- 函数参数和返回值是否有明确的类型声明
- API 响应数据是否有完整的类型定义
- 组件 Props 和 Emits 是否有类型定义
- 事件处理器类型是否正确（如 `MouseEvent`、`KeyboardEvent`）
- 数组和 Promise 类型是否指定了正确的泛型

### 类型设计质量
- 是否正确使用联合类型而非 `any`
- 泛型约束是否足够严格（避免 `<T = any>`）
- 可选属性是否过多（考虑是否应该拆分类型）
- 是否使用了 `unknown` 但缺少类型守卫
- 枚举使用是否合理（考虑使用联合类型替代）
- 是否有未使用的类型定义

---

## 代码质量检查

### 代码整洁性
- 是否有未使用的导入、变量、函数
- 是否有调试输出（`console.log`、`debugger`、`alert` 等）
- 是否有注释掉的代码块
- 是否有 TODO 或 FIXME 注释未处理

### 代码结构
- 函数复杂度是否过高（单个函数不应超过 50 行）
- 文件长度是否合理（不超过 300 行）
- 是否有重复代码需要提取为工具函数
- 组件职责是否单一

### 命名和常量
- 变量和函数命名是否语义化和一致
- 是否有魔法数字和硬编码值
- 常量是否正确定义在 constants 目录
- 布尔变量是否使用动词前缀（is、has、can 等）

---

## 性能优化检查

### Vue 性能优化
- 是否合理使用了 Vue 的优化特性（`v-memo`、`shallowRef`、`shallowReactive`）
- 组件是否合理使用 `v-if` 和 `v-show`
- 列表渲染是否正确使用 `key`
- 大列表是否使用虚拟滚动或分页
- 是否避免了不必要的响应式数据

### 资源和渲染优化
- 图片资源是否优化（格式、大小、懒加载）
- 是否有大量 DOM 操作导致的性能问题
- 是否有不必要的重渲染
- 异步组件是否合理使用动态导入

### 内存管理
- 是否有内存泄漏风险（未清理的定时器、事件监听器、Watcher）
- 组件销毁时是否正确清理资源
- 是否正确使用 `nextTick` 和避免过度使用

---

## 安全性检查

### XSS 防护
- 是否存在 XSS 风险（`v-html` 使用、用户输入处理）
- 用户输入是否进行了适当的转义和验证
- 动态内容渲染是否安全

### 数据安全
- 是否正确处理敏感数据（token、密码等不应在控制台输出）
- API 调用是否有适当的权限验证
- 本地存储是否加密敏感信息

### 输入验证
- 表单输入是否有防注入处理
- 文件上传是否有类型和大小限制
- URL 参数是否进行了验证

---

## 错误处理和用户体验

### 错误处理
- 是否有完善的错误边界处理
- 网络请求是否有超时和重试机制
- 异步操作是否有错误捕获
- 错误信息是否对用户友好

### 用户体验
- 是否有合适的加载状态和骨架屏
- 表单验证是否完整和用户友好
- 操作反馈是否及时（成功、失败提示）
- 长时间操作是否有进度指示

---

## 数据处理检查

### 数据类型和验证
- 接口数据是否有类型校验和运行时验证
- 是否正确处理 null、undefined 和空数据
- 数据转换是否安全（JSON.parse 等）

### 状态管理
- 是否正确处理异步数据状态（loading、error、success）
- 数据缓存策略是否合适
- 表单数据是否正确处理（深拷贝、重置、默认值）

### 分页和搜索
- 分页逻辑是否合理（边界处理、总数显示）
- 搜索功能是否有防抖处理
- 数据筛选是否正确

---

## 依赖和版本管理

### 依赖检查
- 是否引入了不必要的依赖
- 依赖版本是否存在安全漏洞
- 是否有循环依赖
- 第三方库使用是否合理

### 模块化
- 模块导入导出是否规范
- 是否有合理的代码分割策略
- 工具函数是否正确封装

---

## 国际化检查（如项目支持）
- 是否正确使用了 i18n
- 硬编码的文本是否已提取到语言包
- 日期时间格式是否本地化
- 货币和数字格式是否正确

---

## 响应式适配检查
**注意：根据项目类型选择性检查**

### PC端项目（项目名包含 pc 或明确为桌面端）
- 是否支持常见桌面分辨率（1920x1080、1366x768 等）
- 是否处理了浏览器窗口缩放
- 表格和长内容是否有合适的滚动处理
- 是否考虑了大屏显示效果
- 鼠标悬停状态是否完善

### 移动端项目（项目名包含 mobile/m/app 或明确为移动端）
- 是否适配主流移动设备尺寸
- 触摸交互是否友好（按钮大小、间距）
- 是否处理了横竖屏切换
- 性能在移动设备上是否可接受
- 是否支持手势操作

### 响应式项目（需要同时支持PC和移动端）
- 断点设置是否合理
- 组件在不同屏幕尺寸下是否正常显示
- 交互方式是否适配不同设备
- 字体大小是否在不同设备上合适

---

## 可访问性检查（A11y）
- 是否有适当的 ARIA 标签
- 键盘导航是否友好
- 颜色对比度是否符合标准（至少 4.5:1）
- 图片是否有 alt 属性
- 表单是否有合适的 label
- 焦点状态是否清晰可见

---

## 构建和部署检查

### 构建优化
- 打包产物大小是否合理（单个 chunk 不超过 500KB）
- 是否有合适的代码分割策略
- 静态资源是否正确配置 CDN
- 是否启用了 gzip 压缩

### 环境配置
- 环境变量配置是否正确
- 静态资源路径是否正确
- 是否正确配置了缓存策略
- 开发和生产环境配置是否区分

---

## 检查符号说明
- **必须检查** - 关键质量指标
- **建议检查** - 重要但可酌情处理
- **严格禁止** - 发现即需修复